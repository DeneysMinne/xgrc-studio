{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "if [ -n \"$STORAGE_PATH\" ]; then mkdir -p $STORAGE_PATH/uploads/logos $STORAGE_PATH/uploads/images && cp -n public/uploads/logos/* $STORAGE_PATH/uploads/logos/ 2>/dev/null || true; fi && npx prisma migrate deploy && npm start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
